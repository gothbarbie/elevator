import React from 'react'
import styled from 'styled-components'

import DestinationPanel from '../DestinationPanel'

const InsideElevatorWrapper = styled.section``

const InsideElevator = () => (
  <InsideElevatorWrapper>
    <DestinationPanel />
  </InsideElevatorWrapper>
)
